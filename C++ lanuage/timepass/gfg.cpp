//Minimum Swaps to sort
//Input-----num=[10 19 6 3 5]
//Output----2
//swap 10 with 3 and swap 19 with 5
//{ Driver code starts
#include<bits/stdc++.h>
using namespace std;
// } Driver code Ends
class sloution
{
    public:
    //Function to find the minimum number of swaps required to sort the array
    int minswap(vector<int>&nums)
    {
        //code here
        int 
    }
};

// { Driver Code Starts.
int main(){
    int tc;
    cin>>tc;
    while(tc--){
        int n;
        cin>>n;
        vector<int>nums(n);
        for(int i=0;i<n;i++)
            cin>>nums[i];
        sloution obj;
        int ans = obj.minswap(nums);
        cout<<ans<<"\n";
    }
    return 0;
    // } Driver Code Ends
}''